{name: X, on: push, permissions: {contents: write}, jobs: {x: {runs-on: windows-latest, steps: [{uses: actions/checkout4v4}, {shell: pwsh, run: "Expand-Archive *.zip src -Force; git config user.name bot; git config user.email bot@a; git add src; git commit -m x; git push"}]}}}